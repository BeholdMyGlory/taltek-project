<?xml version="1.0" encoding="UTF-8"?>
<vxml version="2.0" lang="en">
    <form>
        <block>
            <prompt>
                Hello. The current date is the {{ day }}{{
                   ("th" if 11 <= day <= 19 else
                    "st" if day % 10 == 1 else
                    "nd" if day % 10 == 2 else
                    "rd" if day % 10 == 3 else
                    "th")
                }} of {{ month }},
                and the time is currently {{ hour }}
                hour{{ "s" if hour != 1 else "" }} and {{ minute }}
                minute{{ "s" if minute != 1 else "" }}.
            </prompt>
        </block>
    </form>
</vxml>
